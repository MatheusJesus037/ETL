# ETL - Qualidade de Dados de Produtos

Projeto que implementa um pipeline ETL para limpeza, padronização e análise de qualidade de dados de produtos.

Como usar:
1. pip install -r requirements.txt
2. python src/etl.py --input data/produtos_raw.csv --out-csv data/produtos_tratados.csv --sqlite data/produtos.db
