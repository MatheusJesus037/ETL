ETL - Qualidade de Dados de Produtos
Projeto que implementa um pipeline ETL para limpeza, padronização e análise de qualidade de dados de produtos.

Como usar:

Instalação de PIP -R requirements.txt
Python SRC/etl.py --dados de entrada/produtos_raw.csv --dados de saída CSV/produtos_tratados.csv --dados sqlite/produtos.db
